<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.thumb2">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/ThumbStyleTwo"
        tools:replace="android:theme">
        <activity android:name=".QrScannerActivity"></activity>
        <activity android:name=".MainScreenActivity" />
        <activity android:name=".DriverDetailsActivity" />
        <activity android:name=".ExceptionDebug" />
        <activity
            android:name=".RegistrationActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".SoldierOrDriverOptionsActivity" />
        <activity
            android:name=".SignUpActivity"
            android:theme="@style/ThumbStyleTwo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:theme="@style/ThumbStyleTwo" />
    </application>

</manifest>